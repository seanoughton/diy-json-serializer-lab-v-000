<div class="productName"><%= @product.name %></div>
<a href="#" class="js-next" id="Next Product" data-id="<%=@product.id%>">Next Product</a>
<p class="productPrice"><%= @product.price %></p>
<p class="postInventory"><%= @product.inventory %></p>
<p class="postDescription"><%= @product.description %></p>

<script type="text/javascript" charset="utf-8">
$(function () {
  $(".js-next").on("click", function() {
    var nextId = parseInt($(".js-next").attr("data-id")) + 1;
    $.getJSON("/products/" + nextId + "/data", function(data) {
      $(".productName").text(dataß["name"]);
      $(".productPrice").text(data["price"]);
      $(".productInventory").text(data["inventory"]);
      $(".productDescription").text(data["description"]);
      // re-set the id to current on the link
      $(".js-next").attr("data-id", data["id"]);
    });
  });
});
</script>
